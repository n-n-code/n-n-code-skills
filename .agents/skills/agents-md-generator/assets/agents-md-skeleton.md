# AGENTS.md

<!-- Skeleton for greenfield AGENTS.md files. Delete any section that does not
apply, replace every placeholder with repo-verified facts, state when each
command applies, keep the file operational, and delete this comment. -->

## Repo purpose and structure

- One or two lines on what this repo is.
- Key directories an agent must understand: `<dir>` — `<why it matters>`.

## Setup

- `<install / bootstrap commands>`

## Build, test, and validate

- `<change type or condition>`: `<command>`
- Full pre-commit or PR check, only if repository policy defines one: `<command>`

## Coding and testing expectations

- `<conventions an agent must follow, grounded in repo config>`

## Boundaries and hazards

- Generated or vendored code: `<paths and rules>`
- Do not: `<forbidden or destructive actions>`

## Deeper docs

- See `<path>` for `<topic>`.
